화물 알뜰운송(합짐/혼적) 서비스의 핵심은 결국 **'공간(적재율)'과 '시간(경로)'의 최적 조합**을 찾아내는 것이겠네요. 1톤 트럭 단일 차종으로 한정하신 점은 변수를 줄여 알고리즘의 정확도를 높이기에 아주 좋은 선택입니다.

작성해주신 기획안을 바탕으로, 알고리즘의 논리 구조와 가중치 설정 방안을 정리해 드립니다.

---

## 1. 데이터 전처리 및 콜 생성 (Input)

* **콜 생성기:** 서울 내 랜덤 좌표 기반, 1톤 트럭 기준의 상/하차지 설정.
* **금액 산정:** 기본 거리 기반 운임 + 물품 종류별 할증(가전/가구 등 무게 및 파손 위험 고려).
* **시간 제약:** 20시 이전 데이터 수집 완료, 22시 최종 루트 확정(취소 불가).

## 2. 합짐(Bundling) 알고리즘 설계

단순히 가까운 곳을 묶는 것이 아니라, **'경로 이탈 최소화'**가 핵심입니다.

### A. 필터링 조건 (Hard Constraints)

먼저 아래 조건을 만족하지 못하면 후보군에서 제외합니다.

1. **적재용량:** $\sum \text{물품 부피} \le \text{1톤 적재함 크기}$ 및 $\sum \text{물품 무게} \le 1,100\text{kg}$ (안전 여유치).
2. **시간 윈도우:** 상차 시간 $A <$ 상차 시간 $B <$ 하차 시간 $A$ (또는 B) 등 운송 가능 순서 확인.
3. **마감 임계치:** 총 운송 시간 21시 이내 완료, 총 운임 20만원 이하.

### B. 가중치 기반 점수화 (Scoring Function)

후보군 중 최적의 조합을 선택하기 위해 각 루트에 점수를 부여합니다. 점수가 낮을수록(비용 중심) 혹은 높을수록(효율 중심) 채택합니다.

$$Score = w_1(\Delta \text{Distance}) + w_2(\Delta \text{Time}) + w_3(1 - \text{Load Factor})$$

* **거리($w_1$):** 기존 개별 운송 시 총 거리 vs 합짐 시 총 이동 거리의 차이.
* **대기 시간($w_2$):** 하차 후 다음 상차까지의 공차 대기 시간 최소화.
* **적재율($w_3$):** 1톤 차량을 얼마나 꽉 채웠는가 (공간 효율성).

## 3. 경로 최적화 로직 (TSP/VRP 응용)

100개의 콜을 일일이 대조하면 연산이 과해질 수 있으므로 **Cluster-First, Route-Second** 방식을 추천합니다.

1. **클러스터링:** 상차지가 비슷한 지역(구 단위)끼리 먼저 그룹화.
2. **루트 편성:** 클러스터 내에서 **Greedy 알고리즘** 혹은 **Dijkstra**를 응용해 '상차A -> 상차B -> 하차A -> 하차B' 등의 최단 경로 산출.

---

## 4. 기대효과 시각화 포인트

기존 대비 효율을 증명하려면 아래 지표를 대시보드에 포함해야 합니다.

| 지표 | 기존 (단독 배송) | 알뜰운송 (합짐) | 기대 효과 |
| --- | --- | --- | --- |
| **총 주행 거리** | $150\text{km}$ | $110\text{km}$ | 유류비 약 **25% 절감** |
| **차량당 수익** | $80,000$원 | $140,000$원 | 기사님 순이익 증대 |
| **고객 부담금** | $80,000$원 | $65,000$원 | 사용자 비용 **15~20% 할인가** 제공 |
| **탄소 배출량** | 고정값 $X$ | $0.7X$ | 친환경 물류 달성 |

---

## 5. 구현을 위한 추가 팁

* **물품 크기 계산:** 가전, 가구 등은 규격화된 '유닛(Unit)' 점유율로 환산하세요. (예: 소형 냉장고 = 0.3 Unit, 의자 = 0.1 Unit)
* **실시간성:** 22시 확정 전까지 새로운 콜이 들어올 때마다 기존 배치된 루트에 '끼워넣기'가 가능한지 판단하는 로직이 있으면 훨씬 정교해집니다.

이 알고리즘을 바탕으로 직접 코드를 작성하시거나 시뮬레이션을 돌려보실 계획인가요? 필요하시다면 **특정 언어(Python, JavaScript 등)로 된 간단한 로직 예시**를 짜드릴 수 있습니다. 어떻게 도와드릴까요?